meow(moving);
fwd(1);
bkd(2); // back 2 steps 
rtt(90); // turn 90 degrees rt 
ltt(pi/2); // turn pi/2 rad left
rtd(1);     // Strafing???
ltd(1);

// do i want to implement jump? // lets keep things 2d for now
 jump();
