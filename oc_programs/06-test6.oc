// $Id: 06-test6.oc,v 1.2 2021-12-08 10:14:36-08 - - $

#include "oclib.h"

struct foo {};
struct bar {};

int f0();
int f1 (int a);
int f2 (int a, int b);
int f3 (string a, string b, string c);
int f4 (ptr<struct foo> a, ptr<struct bar> b);

int main() {
   string s = "a";
   array<string> sa = alloc<array<string>>(10);
   return 0;
}

