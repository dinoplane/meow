// $Id: 11-numbers.oc,v 1.3 2021-12-08 10:14:36-08 - - $

#include "oclib.h"

int main() {
   int number = 1;
   bool done = false;
   while (not done) {
      putint (number);
      putchr ('\n');
      if (number <= 0) done = true;
      number = number + number;
   }
   return 0;
}

